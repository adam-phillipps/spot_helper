require 'spec_helper'

describe SpotHelper do
  it 'has a version number' do
    expect(SpotHelper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
